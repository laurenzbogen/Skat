skat.laurenzbogen.com {
    # Dash/Plotly internal routes — must go to dashboard
    handle /_dash-* {
        reverse_proxy dashboard:8050
    }

    # Your dashboard prefix
    handle /leaderboard* {
        reverse_proxy dashboard:8050
    }

    # Everything else → Grist
    handle {
        reverse_proxy grist:8484
    }
}
